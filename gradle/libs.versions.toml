[versions]
agp = "8.13.2"
kotlin = "2.3.21"
composeBom = "2026.06.01"
coreKtx = "1.18.0"
activityCompose = "1.13.0"
lifecycle = "2.10.0"
kotlinxCoroutines = "1.10.2"
# Stellar（Shizuku 分支，见 INTEGRATION_GUIDE.md）：只发在 JitPack 上，1.0.3 是当前最新
stellar = "1.0.3"
# Dhizuku-API：Maven Central 上有。**不能用 2.5.4+**——从那个版本起制品是 Java 21 字节码
# （class file 65），本机 JDK 17 的编译/打包链读不了；2.5.3 是 Java 8 字节码，
# 且 IDhizuku 的事务码（bindUserService=12/unbindUserService=13）与 2.12.0 服务端一致。
dhizuku = "2.5.3"
junit = "4.13.2"

[libraries]
androidx-compose-bom = { module = "androidx.compose:compose-bom", version.ref = "composeBom" }
androidx-compose-ui = { module = "androidx.compose.ui:ui" }
androidx-compose-ui-graphics = { module = "androidx.compose.ui:ui-graphics" }
androidx-compose-ui-tooling = { module = "androidx.compose.ui:ui-tooling" }
androidx-compose-ui-tooling-preview = { module = "androidx.compose.ui:ui-tooling-preview" }
androidx-compose-material3 = { module = "androidx.compose.material3:material3" }
androidx-compose-foundation = { module = "androidx.compose.foundation:foundation" }

androidx-core-ktx = { module = "androidx.core:core-ktx", version.ref = "coreKtx" }
androidx-activity-compose = { module = "androidx.activity:activity-compose", version.ref = "activityCompose" }
androidx-lifecycle-runtime-ktx = { module = "androidx.lifecycle:lifecycle-runtime-ktx", version.ref = "lifecycle" }
androidx-lifecycle-viewmodel-compose = { module = "androidx.lifecycle:lifecycle-viewmodel-compose", version.ref = "lifecycle" }

kotlinx-coroutines-core = { module = "org.jetbrains.kotlinx:kotlinx-coroutines-core", version.ref = "kotlinxCoroutines" }
kotlinx-coroutines-android = { module = "org.jetbrains.kotlinx:kotlinx-coroutines-android", version.ref = "kotlinxCoroutines" }

# JitPack 的多模块坐标：聚合 pom 是 com.github.roro2239:Stellar-API，
# 它会带出 api / provider / shared / aidl 四个 aar（groupId 是 com.github.roro2239.Stellar-API）
stellar-api = { module = "com.github.roro2239:Stellar-API", version.ref = "stellar" }
# Dhizuku（设备所有者委托）：Maven Central / 阿里云镜像都有，不需要 JitPack
dhizuku-api = { module = "io.github.iamr0s:Dhizuku-API", version.ref = "dhizuku" }

junit = { module = "junit:junit", version.ref = "junit" }

[plugins]
android-application = { id = "com.android.application", version.ref = "agp" }
kotlin-android = { id = "org.jetbrains.kotlin.android", version.ref = "kotlin" }
kotlin-compose = { id = "org.jetbrains.kotlin.plugin.compose", version.ref = "kotlin" }
